@smarttourist/api-client

Tiny universal fetch wrapper. Use in node and browser contexts.

Install locally (for services):
npm install ../libs/api-client

API:
get(path, { baseUrl })
post(path, body, { baseUrl })
put(path, body, { baseUrl })
del(path, { baseUrl })